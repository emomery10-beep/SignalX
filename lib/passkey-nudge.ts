// Shared key for the "don't show the passkey nudge again" flag, read/written
// by both the sign-in page (sign-in path) and the onboarding page (signup
// path, shown once after onboarding completes instead of before it starts).
export const PASSKEY_NUDGE_DISMISSED_KEY = 'askbiz_passkey_nudge_dismissed'
