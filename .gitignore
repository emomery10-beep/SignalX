# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
.next-verify/
out/
build/
dist/

# Environment variables — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Supabase local
supabase/.branches
supabase/.temp

# OS
.DS_Store
Thumbs.db

# CI workflow — pushing it needs a token with `workflow` scope, so it's kept out
# of the local push (the i18n pipeline's `git add -A` was re-tracking it and
# breaking pushes). Add/edit it via the GitHub web UI instead.
.github/workflows/i18n-audit.yml

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts
.vercel
.env*.local
.env*
